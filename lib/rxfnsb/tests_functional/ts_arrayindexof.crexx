/* Focused Level B ARRAYINDEXOF tests. */
options levelb
import rxfnsb

array = .string[]
array[1] = "Alpha"
array[2] = ""
array[3] = "Beta"
array[4] = "Alpha"

errors = 0
errors = errors + check(1, arrayindexof(array, "Alpha"), "first match")
errors = errors + check(4, arrayindexof(array, "Alpha", 2), "duplicate from")
errors = errors + check(3, arrayindexof(array, "Beta"), "last unique match")
errors = errors + check(2, arrayindexof(array, ""), "empty-string match")
errors = errors + check(1, arrayindexof(array, "Alpha", 0), "low start clamps")
errors = errors + check(0, arrayindexof(array, "Alpha", 5), "high start miss")
errors = errors + check(0, arrayindexof(array, "alpha"), "sensitive miss")
errors = errors + check(1, arrayindexof(array, "alpha", 1, 0), "insensitive match")
errors = errors + check(0, arrayindexof(array, "missing", 1, 0), "insensitive miss")
errors = errors + check_int(4, array[0], "input high-water preserved")
errors = errors + check_string("Beta", array[3], "input value preserved")

empty = .string[]
errors = errors + check(0, arrayindexof(empty, ""), "empty array")
errors = errors + test_invalid_case(-1)
errors = errors + test_invalid_case(2)

if errors = 0 then say "PASS: arrayindexof"
return errors <> 0

test_invalid_case: procedure = .int
  arg invalid = .int
  values = .string[]
  values[1] = "A"
  do
    ignored = arrayindexof(values, "A", 1, invalid)
  on signal invalid_arguments
    return 0
  end
  say "FAIL: ARRAYINDEXOF invalid case did not signal" invalid
  return 1

check: procedure = .int
  arg expected = .int, actual = .int, label = .string
  if expected = actual then return 0
  say "FAIL: ARRAYINDEXOF" label "expected" expected "got" actual
  return 1

check_int: procedure = .int
  arg expected = .int, actual = .int, label = .string
  if expected = actual then return 0
  say "FAIL: ARRAYINDEXOF" label "expected" expected "got" actual
  return 1

check_string: procedure = .int
  arg expected = .string, actual = .string, label = .string
  if expected == actual then return 0
  say "FAIL: ARRAYINDEXOF" label "expected" expected "got" actual
  return 1
