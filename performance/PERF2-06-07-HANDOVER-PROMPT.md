# PERF2-06/07 combined Mac-slice handover prompt

> **Completed 2026-07-27.** This is the preserved execution prompt for the
> closed Apple slice; do not paste it to restart the work. The accepted result,
> proportional closeout and successor-hardware instructions are in
> [`evidence/2026-07-27-perf2-06-07-v1r01-r1-closeout/`](evidence/2026-07-27-perf2-06-07-v1r01-r1-closeout/).

Historical recommended setting: **Ultra** for the current attribution, ownership analysis,
representation audit and complete candidate panel. After Adrian selects one
proved implementation slice, **Very High** is normally sufficient for its
focused PoC and first Release verdict.

Copy the prompt below into a new session.

---

You are working in `/Users/adrian/CLionProjects/CREXX` on the combined
**PERF2-06/07 Mac value and VM-ownership slice**.

Finish the Apple-relevant value/reference, copy/move/clear, conversion,
representation, allocation and payload/frame-ownership analysis as one
resumable activity. First reproduce and disposition the open representation
correctness regression, then refresh the current-product attribution and
complete the whole bounded candidate/placement panel. Present the ranked panel
to Adrian and stop before the first production performance edit.

This is not authorization to retry rejected frame-reset or allocation-ledger
designs, complete the cross-platform code-layout decision, select the final VM
stream/default, or begin PERF2-08/09. Only continue past the panel after Adrian
selects an exact candidate. Take each approved candidate separately through the
mandatory first ordinary profiling-off Release verdict and stop again.

## Accepted starting point

At planning time the branch was `develop`, and HEAD and `origin/develop` were
aligned at:

`53f7757c5` (`Merge remote-tracking branch 'origin/develop' into develop`)

The combined roadmap, worklist and this prompt are a later documentation-only
commit. Verify the live branch, HEAD, upstream, worktree and linked worktrees;
do not assume either hash remains current.

The accepted VM-C1b production implementation is:

`a39608426e2c1bb84d5fc0c4f767f4c9492339a9`

The PERF2-06 tactical closeout is:

`d4dd0d84a5093f5530a86e63b75a943e707eaed1`

Accepted boundaries:

- VM-C1b shares interrupt-policy state across frames and retains explicit
  code-layout debt because optimized `rxbvm` Sieve regressed while `rxvm`
  improved. The debt is recorded rather than explained away.
- C2-A, C2-B, C2R01 and C3R01 were rejected and restored. Do not retry a hot
  mapping ledger, fixed-core reset, reset-needed flag, exact reset list,
  quickened clearing derived from that reset design, changed-only numeric
  synchronization or cleanup-only flattened-interpreter reshaping.
- `PERF2-06-D01`, native compact/hot-cold stream selection and the final VM
  recommendation remain open for the supported hardware/compiler matrix. They
  are not Mac implementation work in this slice.
- C2R03 is analysis-only. It may enter the panel only if current payload
  capacity, lifetime and high-water evidence proves a materially different
  procedure-affine slab mechanism with no ordinary mapping-write ledger.
- PERF2-07-V3-R01 is an open correctness issue: initialize a string as `""`,
  copy decimal `"2.2"` into the same destination, perform `dtos`, then `strlen`;
  the required codepoint length is `3`, while the retained observation is stale
  `0`. Audit sibling in-place string-producing conversions.
- Historical PERF2-01/02 profiles are orientation. Re-rank the current product
  after all accepted PERF2-02 through PERF2-06 changes; a historical hot count
  is not current selection evidence.

## Mandatory reading and authority order

Read before task actions:

1. repository `AGENTS.md`;
2. `performance/AGENTS.md`;
3. `performance/ROADMAP.md`, especially the live register, approved remaining
   platform sequence, PERF2-06/07, PERF2-08/09 and Gates D-F;
4. `performance/PERF2-06-07-WORKLIST.md` in full and resume at its first
   unchecked item;
5. `performance/PERFORMANCE-GOVERNANCE.md` and `performance/README.md`;
6. the relevant PERF2-01 dossiers and candidate panels, PERF2-02 direct
   reference evidence/worklist, PERF2-05 value/relink evidence and PERF2-06
   worklist, VM-C1b evidence, code-layout debt, reset rejection and C3 tactical
   rejection;
7. `docs/ai-context/RXVM_INTERPRETER.md`, the relevant value/runtime/compiler/
   assembler documentation and current implementation/tests; and
8. `docs/ai-context/CREXX_LEVELB_AUTHORING.md` before writing or editing any
   maintained `.crexx` analysis or orchestration tool.

Use the live roadmap and current source over the dated programme charter. Keep
the charter and closed evidence historical. Preserve user changes and keep
diagnostic, ordinary Release and every PoC build independently identifiable.

## Required live freeze

Before analysis or measurement:

1. record branch, HEAD/upstream, dirty scope and linked worktrees;
2. verify the checksums and exact commits of every retained baseline/evidence
   package actually used;
3. record Apple model/CPU, macOS, logical CPUs, AC/low-power/thermal/load state,
   compiler, CMake, Ninja and all relevant build options;
4. identify clean ordinary profiling-off Release, profiling/count and native
   sampling products without sharing mutable artifacts;
5. verify current `sizeof(value)`, alignment, key offsets, `stack_frame` size
   and side structures on Apple ARM64; and
6. freeze source, RXAS, RXBIN, linked-image, workload and library hashes for
   every selected cell.

Do not rerun a valid retained baseline for freshness alone. Add only a bounded
same-session control when a decision requires drift qualification. Redirect
verbose build/profile output to temporary logs and inspect focused slices.

## Stage 0 — correctness prerequisite

Reproduce PERF2-07-V3-R01 in optimized and no-opt form on both `rxvm` and
`rxbvm`. Establish the exact string bytes, codepoint length and every validity
or cached-representation field before and after `dcopy`, `dtos` and `strlen`.

Audit sibling in-place operations that replace or expose string bytes while
possibly retaining byte/codepoint length, numeric, decimal, binary, object or
reference validity state. Cover typed null, an initially non-empty destination,
Unicode/codepoint length, aliases/references and both VMs. Separate the
correctness fix from any performance claim and include its exact invalidation
contract in the later panel.

If a language, public RXAS/RXBIN or ABI decision is exposed, stop for Adrian.
Otherwise add the smallest distinguishing regression and correct the bug before
using affected conversion/representation measurements. Validate it narrowly;
do not use the correctness fix as implicit authorization for broad optimization.

## Stage 1 — current combined attribution

Start with current optimized/no-opt Bounce, Richards, Base64 and RexxCPS cells,
then use Sieve and Permute as generic layout/call guards. Add Storage, Towers,
List or JSON only when current PERF2-08 comparability status and the selected
mechanism make the cell valid.

For both VMs attribute at least:

- general and typed copy/move/clear/reset operations and bytes by payload shape;
- reference creation, root/tree/cell/link/relink/invalidation/release by owner
  frame, procedure and caller where possible;
- conversion/materialization counts and retained-representation hits, misses
  and invalidations;
- allocations/frees and bytes by object/shape, lifetime, size class, high-water
  and reuse;
- frame entry/return work only where the selected payload/reference contract
  causes it;
- top native helper/caller stacks and procedure/opcode paths; and
- source/image/product size, steady-state time, lifecycle, RSS and teardown as
  separate dimensions.

Use maintained cREXX Level B tools where new orchestration or analysis code is
needed. Native host samplers may remain diagnostic inputs. A zero current
mechanism count rejects that hypothesis.

## Stage 2 — complete candidate and placement panel

Complete every row in `PERF2-06-07-WORKLIST.md` before selecting production
work:

- `PERF2-06-07-V1R01`: compiler/RXAS elimination or direct result placement;
- `PERF2-06-07-V1R02`: residual direct reference-helper/root/tree reduction;
- `PERF2-06-07-V2R01`: proved payload-shape copy/move/clear/reset fast path;
- `PERF2-06-07-V3R01`: correct representation validity and selective cache
  retention;
- `PERF2-06-07-V5R01`: size/shape-specific capacity reuse or pooling;
- `PERF2-06-07-C2R03`: conditional procedure-affine non-moving value slabs
  plus a compact linear control stack; and
- `PERF2-06-07-V6R01`: hot/cold `value` or broad representation/layout split.

For each row state exact mathematical/semantic correctness, current machine
ceiling, earliest safe owner, lifecycle/ownership rules, both-VM behavior,
fallback/failure behavior, ABI/platform blast radius, decisive cell and
reject/defer/advance disposition. Compare the status quo, earliest safe static
owner, narrow helper/representation form and a hand-equivalent/native ceiling
where useful. Do not implement all rows merely because they are listed.

Prefer compiler/RXAS elimination or direct placement when final-stream facts
prove it. Do not assume runtime quickening, pooling, a new allocator or a broad
value split. C2R03 and V6R01 require an explicit architecture decision from
Adrian before implementation.

## First mandatory stop — selection panel

Present to Adrian:

1. the correctness disposition and sibling audit for V3-R01;
2. the current attribution and exact evidence identities;
3. the complete candidate/owner table, including candidates rejected without
   timing;
4. the smallest candidate with a material machine ceiling and its exact target
   and guard cells; and
5. every language, ABI, public-format or architecture decision.

Stop. Do not install a production performance optimization, run broad QA,
polish a rejected PoC, start PERF2-08/09 or commit implementation work before
Adrian selects the next slice.

## Stage 3 — only after an exact selection

For one selected candidate at a time:

1. create an isolated, independently revertible source/build identity;
2. run only the focused semantic/ownership checks required for safe timing;
3. prove reduced exact operations, bytes, allocations or a selected native
   mechanism before formal timing;
4. freeze implementation and build the ordinary profiling-off Release product;
5. run at least 12 balanced paired rounds on the smallest decisive target,
   both VMs and appropriate unrelated/layout controls;
6. report the first Release verdict and stop for accept/rework/revert; and
7. only after acceptance, complete proportional Debug/Release/sanitizer/
   lifecycle/package/RXBIN compatibility QA, documentation and local commit as
   directed.

Use the formal zero-correctness-failure, 1% common-aggregate and 3% comparable
Tier A guards plus the separate lifecycle/RSS/artifact budgets. Do not batch a
neutral or adverse candidate behind a winner.

## Mac closeout and successor handover

After all selected PERF2-06/07 Mac slices are accepted or explicitly closed:

- reconcile the roadmap, combined worklist, technical documentation and exact
  checksummed evidence;
- retain current Apple Tier A, dual-VM, lifecycle, RSS, artifact and zero-work
  layout controls for affected cells;
- record every rejected form so the hardware session cannot rediscover it as a
  new idea;
- freeze the exact clean source candidate, products, workloads, libraries,
  tools, build options and raw Apple samples; and
- prepare reproducible commands and stop/escalation rules for Intel Linux
  GCC/Clang, supported Linux ARM64 and supported Windows.

Do not make a cross-platform or final-superiority claim from the Mac result.
The approved next order is:

1. PERF2-08 equivalence/capability gate on the Mac;
2. PERF2-09 closure, bounded PERF2-10 Apple controls and PERF2-11 Apple
   pre-handover scorecard on the Mac;
3. Intel x86-64 Linux GCC/Clang native counters, the decision-critical
   `PERF2-06-D01` lanes and final evidence-selected tuning;
4. separately required supported Linux ARM64 validation; and
5. the final supported Windows matrix on the same physical x86-64 machine.

The Intel Linux stage is the principal tuning/selection environment. Windows
validates the frozen candidate on the same hardware; a Windows failure reopens
the cross-platform decision. Apple ARM64 is not a substitute for the required
Linux ARM64 Gate E lane. `PERF2-06-D01` closes only when its retained Apple,
Linux x86-64, Linux ARM64 and Windows evidence reconciles.

Do not start any other PERF2 activity in this session.
