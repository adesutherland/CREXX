diff --git a/bin/tests/CMakeLists.txt b/bin/tests/CMakeLists.txt
index 58a7815ae..9d4d06064 100644
--- a/bin/tests/CMakeLists.txt
+++ b/bin/tests/CMakeLists.txt
@@ -471,7 +471,7 @@ crexx_label_smoke_tests(
         crexx_native_default_link_strip
 )
 
-function(crexx_force_raw_diagnostics_for_bin_tests)
+function(crexx_isolate_bin_test_environment)
     get_property(_crexx_tests DIRECTORY PROPERTY TESTS)
     foreach(_crexx_test IN LISTS _crexx_tests)
         get_test_property(${_crexx_test} ENVIRONMENT _crexx_env)
@@ -479,8 +479,9 @@ function(crexx_force_raw_diagnostics_for_bin_tests)
             set(_crexx_env)
         endif()
         list(APPEND _crexx_env "CREXX_DIAGNOSTICS=raw")
+        list(APPEND _crexx_env "CREXX_HOME=${CMAKE_BINARY_DIR}")
         set_tests_properties(${_crexx_test} PROPERTIES ENVIRONMENT "${_crexx_env}")
     endforeach()
 endfunction()
 
-crexx_force_raw_diagnostics_for_bin_tests()
+crexx_isolate_bin_test_environment()
diff --git a/compiler/tests/CMakeLists.txt b/compiler/tests/CMakeLists.txt
index 50272c043..021e125bd 100644
--- a/compiler/tests/CMakeLists.txt
+++ b/compiler/tests/CMakeLists.txt
@@ -4464,7 +4464,8 @@ endforeach()
 add_test(NAME dynamic_interface_load_driver
         COMMAND ${CMAKE_COMMAND} -P ${DYNAMIC_INTERFACE_DRIVER_SCRIPT})
 set_tests_properties(dynamic_interface_load_driver PROPERTIES
-        LABELS "compiler;stage3;driver;interfaces;late-load")
+        LABELS "compiler;stage3;driver;interfaces;late-load"
+        ENVIRONMENT "CREXX_HOME=${CMAKE_BINARY_DIR}")
 add_test(NAME crexxsaa_variables COMMAND test_crexxsaa_variables)
 set_tests_properties(crexxsaa_variables PROPERTIES
         LABELS "compiler;stage3"
@@ -4602,6 +4603,7 @@ string(APPEND RXC_DIAGNOSTIC_LOCALIZATION_CONTENT
        "    OUTPUT_VARIABLE out\n"
        "    ERROR_VARIABLE err\n"
        "    RESULT_VARIABLE res\n"
+       "    ENCODING UTF-8\n"
        "  )\n"
        "  set(full_out \"\${out}\${err}\")\n"
        "  if(NOT res EQUAL 0)\n"
@@ -4627,6 +4629,7 @@ string(APPEND RXC_DIAGNOSTIC_LOCALIZATION_CONTENT
        "    OUTPUT_VARIABLE out\n"
        "    ERROR_VARIABLE err\n"
        "    RESULT_VARIABLE res\n"
+       "    ENCODING UTF-8\n"
        "  )\n"
        "  set(full_out \"\${out}\${err}\")\n"
        "  if(res EQUAL 0)\n"
diff --git a/compiler/tests/nr14_frozen_parse_contract.cmake b/compiler/tests/nr14_frozen_parse_contract.cmake
index 36f618390..91b2268df 100644
--- a/compiler/tests/nr14_frozen_parse_contract.cmake
+++ b/compiler/tests/nr14_frozen_parse_contract.cmake
@@ -15,7 +15,8 @@ function(run_checked description)
             COMMAND ${ARGN}
             OUTPUT_VARIABLE out
             ERROR_VARIABLE err
-            RESULT_VARIABLE result)
+            RESULT_VARIABLE result
+            ENCODING UTF-8)
     if(NOT result EQUAL 0)
         message(FATAL_ERROR "${description} failed:\n${out}${err}")
     endif()
@@ -52,7 +53,8 @@ function(assert_runs image expected)
                 COMMAND "${${runner}}" "${image}"
                 OUTPUT_VARIABLE out
                 ERROR_VARIABLE err
-                RESULT_VARIABLE result)
+                RESULT_VARIABLE result
+                ENCODING UTF-8)
         string(REPLACE "\r\n" "\n" out "${out}")
         string(REPLACE "\r" "\n" out "${out}")
         if(NOT result EQUAL 0 OR NOT out STREQUAL "${expected}")
@@ -122,7 +124,8 @@ execute_process(
         COMMAND "${RXDAS}" "${WORK_DIR}/missing_feature.rxbin"
         OUTPUT_VARIABLE out
         ERROR_VARIABLE err
-        RESULT_VARIABLE result)
+        RESULT_VARIABLE result
+        ENCODING UTF-8)
 if(result EQUAL 0 OR NOT err MATCHES "opcode 405 requires feature flag 0x00000002")
     message(FATAL_ERROR "missing frozen-PARSE feature was not rejected precisely:\n${out}${err}")
 endif()
@@ -134,7 +137,8 @@ execute_process(
         COMMAND "${RXDAS}" "${WORK_DIR}/unknown_feature.rxbin"
         OUTPUT_VARIABLE out
         ERROR_VARIABLE err
-        RESULT_VARIABLE result)
+        RESULT_VARIABLE result
+        ENCODING UTF-8)
 if(result EQUAL 0 OR NOT err MATCHES "unsupported feature flags 0x00000008")
     message(FATAL_ERROR "unknown RXBIN feature was not rejected precisely:\n${out}${err}")
 endif()
@@ -153,7 +157,8 @@ execute_process(
         COMMAND "${RXDAS}" "${WORK_DIR}/invalid_parseplan_missing_feature.rxbin"
         OUTPUT_VARIABLE out
         ERROR_VARIABLE err
-        RESULT_VARIABLE result)
+        RESULT_VARIABLE result
+        ENCODING UTF-8)
 if(result EQUAL 0 OR NOT err MATCHES "opcode 410 requires feature flag 0x00000002")
     message(FATAL_ERROR
             "missing prepared-plan feature was not rejected precisely:\n${out}${err}")
@@ -163,7 +168,8 @@ foreach(runner IN ITEMS RXVM RXBVM)
             COMMAND "${${runner}}" "${WORK_DIR}/invalid_parseplan.rxbin"
             OUTPUT_VARIABLE out
             ERROR_VARIABLE err
-            RESULT_VARIABLE result)
+            RESULT_VARIABLE result
+            ENCODING UTF-8)
     if(result EQUAL 0 OR NOT "${out}${err}" MATCHES "INVALID_ARGUMENTS")
         message(FATAL_ERROR
                 "${runner} accepted an invalid prepared descriptor: rc=${result}\n${out}${err}")
diff --git a/compiler/tests/perf2_04_certified_call.cmake b/compiler/tests/perf2_04_certified_call.cmake
index fea5a5cc5..cacb34d37 100644
--- a/compiler/tests/perf2_04_certified_call.cmake
+++ b/compiler/tests/perf2_04_certified_call.cmake
@@ -10,7 +10,8 @@ function(run_checked label)
         COMMAND ${ARGN}
         OUTPUT_VARIABLE out
         ERROR_VARIABLE err
-        RESULT_VARIABLE res)
+        RESULT_VARIABLE res
+        ENCODING UTF-8)
     if(NOT res EQUAL 0)
         message(FATAL_ERROR "${label} failed: ${out}${err}")
     endif()
@@ -69,7 +70,8 @@ function(run_dual_vm label expected upper_rxbin lower_rxbin length_rxbin
                     "${substr_rxbin}" "${word_rxbin}" "${main_rxbin}"
             OUTPUT_VARIABLE run_out
             ERROR_VARIABLE err
-            RESULT_VARIABLE res)
+            RESULT_VARIABLE res
+            ENCODING UTF-8)
         string(REPLACE "\r\n" "\n" run_out "${run_out}")
         if(NOT res EQUAL 0 OR NOT run_out STREQUAL expected)
             message(FATAL_ERROR "${label} ${vm} mismatch: expected [${expected}], got [${run_out}], stderr [${err}]")
@@ -162,7 +164,8 @@ foreach(vm RXVM RXBVM)
         COMMAND "${${vm}}" "${WORK_DIR}/spoof_upper.rxbin"
         OUTPUT_VARIABLE run_out
         ERROR_VARIABLE err
-        RESULT_VARIABLE res)
+        RESULT_VARIABLE res
+        ENCODING UTF-8)
     string(REPLACE "\r\n" "\n" run_out "${run_out}")
     if(NOT res EQUAL 0 OR NOT run_out STREQUAL spoof_expected)
         message(FATAL_ERROR "spoof provider ${vm} mismatch: expected [${spoof_expected}], got [${run_out}], stderr [${err}]")
@@ -184,7 +187,8 @@ foreach(vm RXVM RXBVM)
         COMMAND "${${vm}}" "${WORK_DIR}/spoof_word.rxbin"
         OUTPUT_VARIABLE spoof_word_out
         ERROR_VARIABLE spoof_word_err
-        RESULT_VARIABLE spoof_word_res)
+        RESULT_VARIABLE spoof_word_res
+        ENCODING UTF-8)
     string(REPLACE "\r\n" "\n" spoof_word_out "${spoof_word_out}")
     if(NOT spoof_word_res EQUAL 0 OR NOT spoof_word_out STREQUAL "Alpha\n")
         message(FATAL_ERROR "spoof word provider ${vm} mismatch: [${spoof_word_out}] [${spoof_word_err}]")
@@ -204,7 +208,8 @@ foreach(vm RXVM RXBVM)
         COMMAND "${${vm}}" "${WORK_DIR}/spoof_lower.rxbin"
         OUTPUT_VARIABLE spoof_lower_out
         ERROR_VARIABLE spoof_lower_err
-        RESULT_VARIABLE spoof_lower_res)
+        RESULT_VARIABLE spoof_lower_res
+        ENCODING UTF-8)
     string(REPLACE "\r\n" "\n" spoof_lower_out "${spoof_lower_out}")
     if(NOT spoof_lower_res EQUAL 0 OR NOT spoof_lower_out STREQUAL "MIXED ÄÖ\n")
         message(FATAL_ERROR "spoof lower provider ${vm} mismatch: [${spoof_lower_out}] [${spoof_lower_err}]")
diff --git a/compiler/tests/perf2_04_inline_assembler_effects.cmake b/compiler/tests/perf2_04_inline_assembler_effects.cmake
index 6b067a5da..367f99878 100644
--- a/compiler/tests/perf2_04_inline_assembler_effects.cmake
+++ b/compiler/tests/perf2_04_inline_assembler_effects.cmake
@@ -15,7 +15,8 @@ foreach(mode opt noopt)
         WORKING_DIRECTORY "${WORK_DIR}"
         OUTPUT_VARIABLE out
         ERROR_VARIABLE err
-        RESULT_VARIABLE res)
+        RESULT_VARIABLE res
+        ENCODING UTF-8)
     if(NOT res EQUAL 0)
         message(FATAL_ERROR "rxc ${mode} failed: ${out}${err}")
     endif()
@@ -51,7 +52,8 @@ foreach(mode opt noopt)
         WORKING_DIRECTORY "${WORK_DIR}"
         OUTPUT_VARIABLE out
         ERROR_VARIABLE err
-        RESULT_VARIABLE res)
+        RESULT_VARIABLE res
+        ENCODING UTF-8)
     if(NOT res EQUAL 0)
         message(FATAL_ERROR "rxas ${mode} failed: ${out}${err}")
     endif()
@@ -62,7 +64,8 @@ foreach(mode opt noopt)
             WORKING_DIRECTORY "${WORK_DIR}"
             OUTPUT_VARIABLE run_out
             ERROR_VARIABLE err
-            RESULT_VARIABLE res)
+            RESULT_VARIABLE res
+            ENCODING UTF-8)
         string(REPLACE "\r\n" "\n" run_out "${run_out}")
         if(NOT res EQUAL 0 OR NOT run_out STREQUAL expected)
             message(FATAL_ERROR "${vm} ${mode} mismatch: expected [${expected}], got [${run_out}], stderr [${err}]")
diff --git a/compiler/tests/perf2_04_inline_assembler_imports.cmake b/compiler/tests/perf2_04_inline_assembler_imports.cmake
index fa0d5cc5a..5d1c805bc 100644
--- a/compiler/tests/perf2_04_inline_assembler_imports.cmake
+++ b/compiler/tests/perf2_04_inline_assembler_imports.cmake
@@ -8,7 +8,8 @@ function(run_checked label)
         COMMAND ${ARGN}
         OUTPUT_VARIABLE out
         ERROR_VARIABLE err
-        RESULT_VARIABLE res)
+        RESULT_VARIABLE res
+        ENCODING UTF-8)
     if(NOT res EQUAL 0)
         message(FATAL_ERROR "${label} failed: ${out}${err}")
     endif()
@@ -40,7 +41,8 @@ function(run_dual_vm label dep_rxbin main_rxbin)
             COMMAND "${${vm}}" "${dep_rxbin}" "${main_rxbin}"
             OUTPUT_VARIABLE run_out
             ERROR_VARIABLE err
-            RESULT_VARIABLE res)
+            RESULT_VARIABLE res
+            ENCODING UTF-8)
         string(REPLACE "\r\n" "\n" run_out "${run_out}")
         if(NOT res EQUAL 0 OR NOT run_out STREQUAL expected)
             message(FATAL_ERROR "${label} ${vm} mismatch: expected [${expected}], got [${run_out}], stderr [${err}]")
diff --git a/compiler/tests/perf2_05_partial_call.cmake b/compiler/tests/perf2_05_partial_call.cmake
index 62b4ee547..41b7e1ebe 100644
--- a/compiler/tests/perf2_05_partial_call.cmake
+++ b/compiler/tests/perf2_05_partial_call.cmake
@@ -8,7 +8,8 @@ function(run_checked label)
         COMMAND ${ARGN}
         OUTPUT_VARIABLE out
         ERROR_VARIABLE err
-        RESULT_VARIABLE res)
+        RESULT_VARIABLE res
+        ENCODING UTF-8)
     if(NOT res EQUAL 0)
         message(FATAL_ERROR "${label} failed: ${out}${err}")
     endif()
@@ -20,7 +21,8 @@ function(run_dual_vm label rxbin)
             COMMAND "${${vm}}" "${rxbin}"
             OUTPUT_VARIABLE run_out
             ERROR_VARIABLE err
-            RESULT_VARIABLE res)
+            RESULT_VARIABLE res
+            ENCODING UTF-8)
         string(REPLACE "\r\n" "\n" run_out "${run_out}")
         if(NOT res EQUAL 0 OR NOT run_out STREQUAL expected)
             message(FATAL_ERROR
@@ -72,7 +74,8 @@ foreach(mode opt noopt)
             COMMAND "${${vm}}" "${WORK_DIR}/array_${mode}.rxbin"
             OUTPUT_VARIABLE array_out
             ERROR_VARIABLE array_err
-            RESULT_VARIABLE array_res)
+            RESULT_VARIABLE array_res
+            ENCODING UTF-8)
         string(REPLACE "\r\n" "\n" array_out "${array_out}")
         if(NOT array_res EQUAL 0 OR NOT array_out STREQUAL array_expected)
             message(FATAL_ERROR
diff --git a/performance/README.md b/performance/README.md
index a82972e21..3c3e65c08 100644
--- a/performance/README.md
+++ b/performance/README.md
@@ -41,11 +41,13 @@ capability decisions and per-benchmark ooRexx closure.
 | `performance/evidence/2026-07-27-perf2-08-qualification/` | Approved Mac capability/equivalence panel: complete non-common qualification, exact source/runtime identities and negative Mandelbrot evidence |
 | `performance/evidence/2026-07-27-perf2-09-mac-closure/` | Formal same-session Mac closure: common-five scorecard, Towers/RexxCPS/lifecycle lanes, RSS/artifacts, workload dossiers and recursive checksums |
 | `performance/evidence/2026-07-28-perf2-10-11-intel-linux/` | Initial Intel Linux x86-64 handover: GCC/Clang correctness and sanitizer closure, formal runtime/RSS/lifecycle/artifacts, schema-5 profiles, native PMU attribution and macOS handback |
+| `performance/evidence/2026-07-29-perf2-11-windows-x86-64/` | Windows x86-64 correctness and formal timing/RSS/lifecycle/artifact scorecard under the supported CLion MinGW toolchain; no profiling or tuning |
 | `performance/evidence/` | Dated provenance, commands, raw samples and summaries |
 | `performance/tools/run_cross_runtime.crexx` | Level B serial capture tool for one workload/runtime cell |
 | `performance/tools/run_lifecycle.crexx` | Level B compile/translate and cold load-to-first-result capture across the three portfolio runtimes |
 | `performance/tools/run_evidence_bundle.crexx` | Level B exact-image timing/profile/RXSEQ bundle orchestration and reporting |
 | `performance/tools/run_cross_runtime_matrix.crexx` | Level B compact formal timing/RSS matrix capture, summary and aggregate reporting |
+| `performance/tools/windows_peak_rss.ps1` | Native Windows child-process peak working-set sampler used by the formal matrix RSS fallback |
 | `performance/tools/inventory_performance_artifacts.crexx` | Level B hash/size inventory for versioned performance artifact manifests |
 | `performance/tools/report_nr09_macro_timings.zsh` | NR-09 all-form component/macro timing and review-ledger report from paired schema-4 profiles |
 | `performance/capability-gaps.md` | Audited missing surfaces and candidates uncovered by portfolio ports |
@@ -78,7 +80,9 @@ versioned manifest groups runtime cells by workload, and the driver rotates the
 cell order per warmup and recorded round. It writes consolidated sample and
 output tables, per-cell median/IQR/MAD/noise summaries, higher-is-better ratios,
 and the four separately named common-portfolio geometric means. RSS capture
-uses the same manifest with zero warmups and remains separate from timing. For
+uses the same manifest with zero warmups and remains separate from timing.
+Unix hosts use `/usr/bin/time`; native Windows uses `windows_peak_rss.ps1` to
+sample the direct child's `PeakWorkingSet64`. For
 an aggregate row, `work` must be a positive integer and the timing summary uses
 process-inclusive `work / elapsed` normalized throughput; raw elapsed time and
 the exact work count remain in `samples.csv`. `--summary-only` with one or more
diff --git a/performance/ROADMAP.md b/performance/ROADMAP.md
index efc5e0cc0..e3461bf83 100644
--- a/performance/ROADMAP.md
+++ b/performance/ROADMAP.md
@@ -292,6 +292,27 @@ bounded dual-VM Clang profile control, 154 non-multiplexed PMU captures and 16
 focused cycle profiles are retained in
 [`2026-07-28-perf2-10-11-intel-linux`](evidence/2026-07-28-perf2-10-11-intel-linux/).
 
+### Windows baseline completion - 2026-07-29
+
+The supported CLion MinGW Windows x86-64 Release product now passes
+1,926/1,926 tests after correcting UTF-8 decoding in six CMake-script tests
+and isolating nine driver/native tests from an inherited stale `CREXX_HOME`.
+No compiler, VM, RXAS or language behavior changed.
+
+The formal same-session Windows scorecard retains dual-VM timing, a governed
+variability append, native child-process peak working set, 20-sample lifecycle
+phases, artifact hashes/sizes and exact ooRexx 5.1.0, Regina 3.9.7, NetRexx
+5.10-GA and Temurin 26.0.1+8 identities. The common-five geometric means are
+2.363219x/1.984737x versus ooRexx and 0.749453x/0.629424x versus decimal
+NetRexx for `rxvm`/`rxbvm`. Richards, Base64, Towers and the separately
+disclosed RexxCPS row remain below ooRexx, so no final superiority or default
+VM claim is made. Evidence:
+[`2026-07-29-perf2-11-windows-x86-64`](evidence/2026-07-29-perf2-11-windows-x86-64/).
+
+No profiling, tuning candidate or rebuild-heavy analysis was performed on the
+Windows host. Gate E remains open for supported Linux ARM64 coverage and the
+later whole-scorecard architecture decision.
+
 No production optimization or compiler/layout option is selected. Richards
 selects general value copying and attribute-storage trimming; Towers selects
 copy/clear/reset/allocation work plus front-end/indirect-branch pressure;
@@ -1480,6 +1501,12 @@ Intel x86-64 hardware under the supported Windows toolchain. Gate E is not met
 by Apple plus Intel/Windows alone: Linux ARM64 remains a required supported
 release-architecture lane.
 
+The 2026-07-29 Windows x86-64 baseline is retained under
+[`2026-07-29-perf2-11-windows-x86-64`](evidence/2026-07-29-perf2-11-windows-x86-64/).
+It completes the requested Windows scorecard lane but does not close Gate E:
+supported Linux ARM64 and a selected cross-platform/default-VM decision remain
+outstanding.
+
 ### Gate F — final external claim
 
 - zero correctness failures;
diff --git a/performance/tools/run_cross_runtime_matrix.crexx b/performance/tools/run_cross_runtime_matrix.crexx
index bb44bd017..34d95ebef 100644
--- a/performance/tools/run_cross_runtime_matrix.crexx
+++ b/performance/tools/run_cross_runtime_matrix.crexx
@@ -97,8 +97,12 @@ main: procedure = .int
     return argument_error("--summary-only requires at least one --samples PATH")
   if measurement = "rss" & warmups <> 0 then
     return argument_error("RSS capture uses --warmups 0")
+  rss_time_command = ""
   rss_time_option = ""
-  if measurement = "rss" then rss_time_option = detect_rss_time_option()
+  if measurement = "rss" then do
+    rss_time_command = detect_rss_time_command()
+    rss_time_option = detect_rss_time_option(rss_time_command)
+  end
 
   manifest_rows = read_input_manifest(manifest_path)
   if manifest_rows[0] = 0 then return execution_error("manifest has no entries")
@@ -134,8 +138,8 @@ main: procedure = .int
       group_end = group_end + 1
     end
 
-    if run_group(manifest_rows, group_start, group_end, measurement, rss_time_option, ,
-        warmups, runs, samples, outputs) <> 0 then do
+    if run_group(manifest_rows, group_start, group_end, measurement, rss_time_command, ,
+        rss_time_option, warmups, runs, samples, outputs) <> 0 then do
       call write_lines output_dir || "/samples.csv", samples
       call write_lines output_dir || "/outputs.csv", outputs
       completed = timestamp_utc()
@@ -241,7 +245,8 @@ validate_manifest: procedure = .int
 
 run_group: procedure = .int
   arg rows = .string[], group_start = .int, group_end = .int, measurement = .string, ,
-      rss_time_option = .string, warmups = .int, runs = .int, ,
+      rss_time_command = .string, rss_time_option = .string, ,
+      warmups = .int, runs = .int, ,
       expose samples = .string[], expose outputs = .string[]
   group_count = group_end - group_start + 1
 
@@ -258,8 +263,8 @@ run_group: procedure = .int
         relative = (position + offset - 1) <mod> group_count
         row_index = group_start + relative
         fields = split_delimited(rows[row_index], "|")
-        result = capture_entry(fields, measurement, rss_time_option, phase, ,
-            round_number, position, samples, outputs)
+        result = capture_entry(fields, measurement, rss_time_command, ,
+            rss_time_option, phase, round_number, position, samples, outputs)
         if result <> 0 then return result
       end
     end
@@ -267,8 +272,8 @@ run_group: procedure = .int
   return 0
 
 capture_entry: procedure = .int
-  arg fields = .string[], measurement = .string, rss_time_option = .string, ,
-      phase = .string, round_number = .int, position = .int, ,
+  arg fields = .string[], measurement = .string, rss_time_command = .string, ,
+      rss_time_option = .string, phase = .string, round_number = .int, position = .int, ,
       expose samples = .string[], expose outputs = .string[]
   entry_id = fields[1]
   workload = fields[2]
@@ -284,8 +289,16 @@ capture_entry: procedure = .int
   command = split_list(fields[12])
   if measurement = "rss" then do
     timed_command = .string[]
-    timed_command[1] = "/usr/bin/time"
-    timed_command[2] = rss_time_option
+    timed_command[1] = rss_time_command
+    if rss_time_command = "powershell.exe" then do
+      timed_command[2] = "-NoLogo"
+      timed_command[3] = "-NoProfile"
+      timed_command[4] = "-ExecutionPolicy"
+      timed_command[5] = "Bypass"
+      timed_command[6] = "-File"
+      timed_command[7] = "performance/tools/windows_peak_rss.ps1"
+    end
+    else timed_command[2] = rss_time_option
     do command_index = 1 to command[0]
       timed_command[timed_command[0] + 1] = command[command_index]
     end
@@ -582,9 +595,22 @@ extract_rss: procedure = .string
   end
   return ""
 
+file_exists: procedure = .int
+  arg path = .string
+  command_out = .string[]
+  command_err = .string[]
+  address crexx "stat :path" output command_out error command_err
+  return rc = 0
+
+detect_rss_time_command: procedure = .string
+  if file_exists("/usr/bin/time") then return "/usr/bin/time"
+  return "powershell.exe"
+
 detect_rss_time_option: procedure = .string
+  arg time_command = .string
+  if time_command = "powershell.exe" then return ""
   command = .string[]
-  command[1] = "/usr/bin/time"
+  command[1] = time_command
   command[2] = "--version"
   child_output = .string[]
   child_error = .string[]
diff --git a/performance/tools/run_lifecycle.crexx b/performance/tools/run_lifecycle.crexx
index 706307d24..2657296aa 100644
--- a/performance/tools/run_lifecycle.crexx
+++ b/performance/tools/run_lifecycle.crexx
@@ -167,11 +167,15 @@ capture_crexx: procedure = .int
   if make_directory(runtime_dir) <> 0 then return 1
   source = repo_root || "/tests/benchmarks/cross-runtime/lifecycle/lifecycle_probe.crexx"
   output_stem = runtime_dir || "/lifecycle_probe"
+  rxc_path = build_dir || "/bin/rxc"
+  if file_exists(rxc_path || ".exe") then rxc_path = rxc_path || ".exe"
+  rxas_path = build_dir || "/bin/rxas"
+  if file_exists(rxas_path || ".exe") then rxas_path = rxas_path || ".exe"
 
   do local_sequence = 1 to runs
     sequence = sequence_offset + local_sequence
     command = .string[]
-    command[1] = build_dir || "/bin/rxc"
+    command[1] = rxc_path
     command[2] = "-x"
     command[3] = "-o"
     command[4] = output_stem
@@ -183,7 +187,7 @@ capture_crexx: procedure = .int
     command[2] = "-E"
     command[3] = "chdir"
     command[4] = runtime_dir
-    command[5] = build_dir || "/bin/rxas"
+    command[5] = rxas_path
     command[6] = "-o"
     command[7] = "lifecycle_probe"
     command[8] = "lifecycle_probe"
@@ -194,7 +198,9 @@ capture_crexx: procedure = .int
       if vm_index = 2 then vm = "rxbvm"
       if crexx_vm <> "both" & crexx_vm <> vm then iterate
       vm_command = .string[]
-      vm_command[1] = build_dir || "/bin/" || vm
+      vm_path = build_dir || "/bin/" || vm
+      if file_exists(vm_path || ".exe") then vm_path = vm_path || ".exe"
+      vm_command[1] = vm_path
       vm_command[2] = output_stem || ".rxbin"
       vm_command[3] = "-a"
       if capture_phase("crexx", vm, "load_first_result", sequence, vm_command, ,
@@ -210,11 +216,15 @@ capture_oorexx: procedure = .int
   if make_directory(runtime_dir) <> 0 then return 1
   source = repo_root || "/tests/benchmarks/cross-runtime/lifecycle/lifecycle_probe.rex"
   image = runtime_dir || "/lifecycle_probe.orx"
+  rexxc_path = oorexx_home || "/bin/rexxc"
+  if file_exists(rexxc_path || ".exe") then rexxc_path = rexxc_path || ".exe"
+  rexx_path = oorexx_home || "/bin/rexx"
+  if file_exists(rexx_path || ".exe") then rexx_path = rexx_path || ".exe"
 
   do local_sequence = 1 to runs
     sequence = sequence_offset + local_sequence
     command = .string[]
-    command[1] = oorexx_home || "/bin/rexxc"
+    command[1] = rexxc_path
     command[2] = source
     command[3] = image
     if capture_phase("oorexx", "oorexx", "translate", sequence, command, "", rows) <> 0 then return 1
@@ -226,7 +236,7 @@ capture_oorexx: procedure = .int
       command[3] = image
     end
     else do
-      command[1] = oorexx_home || "/bin/rexx"
+      command[1] = rexx_path
       command[2] = image
     end
     if capture_phase("oorexx", "oorexx", "load_first_result", sequence, command, ,
@@ -242,24 +252,39 @@ capture_netrexx: procedure = .int
   source = repo_root || "/tests/benchmarks/cross-runtime/lifecycle/lifecycle_probe.nrx"
   local_source = runtime_dir || "/lifecycle_probe.nrx"
   copy_command = .string[]
-  copy_command[1] = "cp"
-  copy_command[2] = source
-  copy_command[3] = local_source
+  copy_command[1] = "cmake"
+  copy_command[2] = "-E"
+  copy_command[3] = "copy"
+  copy_command[4] = source
+  copy_command[5] = local_source
   if run_setup(copy_command) <> 0 then return execution_error("could not stage NetRexx lifecycle source")
 
   do local_sequence = 1 to runs
     sequence = sequence_offset + local_sequence
     command = .string[]
-    command[1] = netrexx_home || "/bin/NetRexxC.sh"
-    command[2] = "-keep"
-    command[3] = "-sourcedir"
-    command[4] = local_source
+    if file_exists(netrexx_home || "/bin/NetRexxC.bat") then do
+      command[1] = "cmd.exe"
+      command[2] = "/d"
+      command[3] = "/c"
+      command[4] = netrexx_home || "/bin/NetRexxC.bat"
+      command[5] = "-keep"
+      command[6] = "-sourcedir"
+      command[7] = local_source
+    end
+    else do
+      command[1] = netrexx_home || "/bin/NetRexxC.sh"
+      command[2] = "-keep"
+      command[3] = "-sourcedir"
+      command[4] = local_source
+    end
     if capture_phase("netrexx", "netrexx", "compile", sequence, command, "", rows) <> 0 then return 1
 
     command = .string[]
     command[1] = "java"
     command[2] = "-cp"
-    command[3] = runtime_dir || ":" || netrexx_home || "/runlib/NetRexxR.jar"
+    classpath_separator = ":"
+    if file_exists(netrexx_home || "/bin/NetRexxC.bat") then classpath_separator = ";"
+    command[3] = runtime_dir || classpath_separator || netrexx_home || "/runlib/NetRexxR.jar"
     command[4] = "lifecycle_probe"
     if capture_phase("netrexx", "netrexx", "load_first_result", sequence, command, ,
         "PASS: Lifecycle Probe", rows) <> 0 then return 1
