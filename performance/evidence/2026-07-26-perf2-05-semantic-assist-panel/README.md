# PERF2-05 semantic-assist selection package

This package records the first mandatory PERF2-05 semantic-assist stop after
accepted P05-CF1. It refreshes the governed optimized/no-opt profile and RXSEQ
evidence, ranks the surviving semantic units, compares placement owners and
retains bounded PoC verdicts. Its status is **decision required**. It does not
authorize or contain a production ladder.

Starting repository state:

- branch: `develop`
- HEAD: `537d3b3d276606767535ecb84ad2a3c80073e5dd`
- subject: `perf: generalize constant call evaluation`
- `origin/develop`: `d1c5245d49c0bd9cc48a7d33ef16f2f4555cc986`
- local branch position: ahead by four commits
- initial worktree: clean

The accepted P05-CF1 package replayed 52/52 SHA-256 entries successfully from
its own bundle root. The current selection work uses detached clean products;
the ordinary checkout contains only evidence/worklist documentation changes.

Package map:

- `CANDIDATE-PANEL.md`: refreshed ranking, semantic obligations and placement
  comparison;
- `MEASUREMENTS.md`: exact instruction and ordinary Release results;
- `VERDICT.md`: recommended separable ladder, neutral/rejected ledger and the
  approval stop;
- `PROVENANCE.md`: repository, product, input and correctness identities;
- `profiles/post-p05/`: complete optimized/no-opt dual-VM profiles and RXSEQ
  N=2/3/4 captures; and
- `poc/`: scratch patch, mechanically transformed images, semantic guard and
  complete dual-VM PoC bundles.

The headline result is that narrow weak-reference-descriptor materialization
is feasible and favorable: it copies the descriptor/reference cell while the
arena continues to own the target object. It is not object materialization or
an owning-reference change. The recommended selection is multiple layers with
compiler/RXAS eligibility proof, private execution where needed and no new
public instruction at this stage.
