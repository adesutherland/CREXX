# PERF3-01 current-HEAD Mac evidence

This compact bundle restores exact current-product Apple timing and focused
mechanism authority for clean detached commit `3f43a0014`. See
[`decision-summary.md`](decision-summary.md) for the decision package.

Adrian accepted this evidence boundary and ranked panel on 2026-07-31,
closing PERF3-01 and opening PERF3-02 for evidence/design and bounded PoCs.

## Outcome

- ordinary profiling-off Release timing: `348/348` initial plus `50/50`
  governed-append executions passed;
- current common-five geometric mean: `2.139811x/1.818954x` versus ooRexx and
  `0.779920x/0.662974x` versus decimal NetRexx for `rxvm`/`rxbvm`;
- focused schema-5 profiles: `10/10` complete across Richards, Towers, Base64,
  RexxCPS and the Sieve guard in both VM modes;
- current ranking: PERF3-02 first, PERF3-05 second, PERF3-03 third, PERF3-04
  evidence-gated;
- production edits, broad platform reruns, candidate selection, commit and
  push: none.

## Evidence map

- `timing/`: immutable initial and append raw samples plus authoritative merged
  summary, ratio and geometric-mean tables;
- `profiles/`: current counts-only schema-5 CSVs, output and consolidated
  mechanism/opcode/procedure/domain summaries;
- `product-validity.csv`: exact PERF2/current product and selected image hashes;
- `gap-mechanism-ledger.csv`: timing gaps, operation/byte counts, retained
  native footprint, machine ceiling, semantic risk and earliest safe owner;
- `artifacts.csv`: exact path, size and SHA-256 inventory for 95 products,
  sources, comparators, manifests, profiles and retained authorities;
- `manifests/`: exact formal and governed append schedules;
- `provenance/`: clean product, host, power, toolchain and campaign boundaries;
- `logs/`: compact matrix-driver result streams; and
- `checksums.sha256`: recursive closure excluding the checksum file itself.

The retained PERF2-09 Mac scorecard remains a valid historical observation.
This bundle is an absolute current-product baseline, not an unmatched-session
before/after verdict. An earlier capture that accidentally routed cREXX cells
to the retired PERF2 product is preserved under `timing-invalid-old-product/`,
explicitly marked invalid and excluded from every figure above.
