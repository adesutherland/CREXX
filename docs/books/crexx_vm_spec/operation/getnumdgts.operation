GETDGTS fetches the number of significant digits in a decimal operation when decimal mode MC is active. Note that the returned number of decimal digits is an integer.\footnote{When decimal mode DC is used, then this instruction will be ignored (but is still a valid instruction in a \code{.rxbin} module).}