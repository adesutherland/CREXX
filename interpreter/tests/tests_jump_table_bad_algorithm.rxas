/*
 * Reserved jump-table algorithms parse but are not executable in the linear slice.
 */

.globals=0

main() .locals=2
    .jtable words acph
    br done
word_key: .jcase words "if"
    ret 0
done:
    load r0,"if"
    jumps r0,words
    ret 0
