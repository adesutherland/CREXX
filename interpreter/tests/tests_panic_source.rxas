/*
 * CREXX Interpreter test for fallback panic source reporting.
 */

.globals=0

main() .locals=1
    .srcstep 1 1 17 "panic_source.rexx" 7 3 20 "bad = 'abc' + 0.0"
    load r0, "abc"
    stof r0
    ret 0
