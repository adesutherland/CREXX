# Plugin demonstrations

This directory is reserved for example native or Rexx plugin packages. Example
plugins must keep their build products below the installed `examples/plugins/`
tree rather than publishing them as default runtime plugins.
