# Function demonstrations

This directory contains callable function modules that demonstrate cREXX
library authoring without becoming part of the supported Level B or Level G
library surface.

Each demonstration owns its source, build instructions, and safety test in a
separate subdirectory. Built modules are installed below the matching
`examples/functions/` path rather than `bin/`.
