# Compiler-exit demonstrations

This directory contains optional compiler-exit examples and any example-only
runtime support they consume. They are built and tested independently from the
default compiler-exit bundle and are installed below `examples/exits/`.
