/* Regression: eager RXPA scan must not re-enter a provider initializer.
   rxvector still returns the Rexx-owned packedfloat class from rxfnsg; the
   former rxstats.linearfit Rexx carrier has moved into its native provider. */
options levelb

main: procedure = .int
  return 0
