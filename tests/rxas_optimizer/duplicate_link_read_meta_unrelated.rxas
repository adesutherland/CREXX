* Optimiser can skip register-token metadata for unrelated registers.

main() .locals=10
    load r0,"payload"

    link r1,r0
    scopy r2,r1
    unlink r1
    .meta "fixture.duplicate_link_read"="unrelated" ".string" r9

    link r3,r0
    scopy r4,r3
    unlink r3

    say r4
    ret
