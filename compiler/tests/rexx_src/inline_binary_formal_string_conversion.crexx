options levelb

import rxfnsb

main: procedure = .int
  arg argv = .string[]

  value = argv[1]
  bytes = binlength(value)
  say "bytes=" || bytes
  if bytes <> 3 then return 1

  say "PASS: inline binary formal string conversion"
  return 0
