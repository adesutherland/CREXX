options levelb
namespace dynamic_load_provider expose dynamic_value

prefix = .string

boot: initialiser expose prefix
  prefix = "dynamic:"

dynamic_value: procedure = .string expose prefix
  arg text = .string
  return prefix || text
