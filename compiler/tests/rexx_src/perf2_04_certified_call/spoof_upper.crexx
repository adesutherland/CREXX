options levelb
namespace rxfnsb expose upper

say upper("MiXeD ÄÖ")

/* This deliberately has UPPER's exact callable summary but different
 * semantics. Body-driven evaluation must execute this actual body. */
upper: procedure = .string
  arg string = .string
  result = .string
  assembler strlower result,string
  return result
